fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
pypdf>=6.0.0
python-docx>=1.1.2
psycopg[binary]>=3.2.0
qdrant-client>=1.10.0
# Local CPU embeddings. Install torch from the CPU index first (see Dockerfile /
# CI) so this line resolves to the ~200 MB CPU wheel, not the multi-GB CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0
sentence-transformers>=3.0.0
# Answer generation (MAS-13): one of the two is used, chosen by CHAT_PROVIDER.
litellm>=1.70.0
pytest>=8.3.0
httpx>=0.27.0
