# Evaluation harness only (MAS-91) — never installed in the api image.
# Ragas pulls langchain, datasets and instructor; keep it in its own venv:
#   py -3.13 -m venv .venv-eval && .venv-eval/Scripts/pip install -r requirements-eval.txt
ragas>=0.4.3,<0.5
# ragas 0.4.3 still imports langchain_community.chat_models.vertexai, removed in 0.4.
langchain-community<0.4
instructor>=1.7
litellm>=1.70.0
httpx>=0.27.0
